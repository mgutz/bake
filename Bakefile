# This is an example Bakefile

_private () {         # underscored functions do not display on task list
    echo in private
}

build () {            # builds the project
    # invokes clean only once
    invoke "clean"
    echo building ...
}

clean () {
    echo cleaning ...
    _private
}


# vim:filetype=sh:
